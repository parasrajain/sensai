import React from 'react'

const MainLayout = ({children}) => {
    // redirect after onboarding
    
  return (
    <div className='mt-24 mb-20 mx-auto container'>{children}</div>
  )
}

export default MainLayout